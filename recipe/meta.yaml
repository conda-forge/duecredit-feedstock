{% set name = "duecredit" %}
{% set version = "0.9.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/duecredit-{{ version }}.tar.gz
  sha256: 0e0fd87e9e46ce6c94308e9f780c203fe836d89628404f8bf5af96a7457bed1c

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  entry_points:
    - duecredit = duecredit.cmdline.main:main

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools >=61.0.0
  run:
    - importlib-metadata
    - citeproc-py >=0.4
    - python >=3.8
    - six
    - requests

test:
  imports:
    - duecredit
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/duecredit/duecredit
  summary: Automated collection and reporting of citations for used software/methods/datasets
  license: BSD-2-Clause
  license_family: BSD
  license_file: LICENSE

extra:
  recipe-maintainers:
    - yarikoptic
    - smoia
    - mscheltienne
